# 실행 방법

1. 이 폴더에서 CMD 또는 PowerShell을 엽니다.
2. 처음 한 번만 실행:

   npm install

3. 개발 서버 실행:

   npm run dev

4. 터미널에 표시되는 주소(보통 http://localhost:5173/)로 접속합니다.

기존 HTML 학습 페이지들은 `public` 폴더에 그대로 보존되어 있으며,
React의 `App.jsx`가 기존 홈페이지(`public/home.html`)를 표시합니다.
